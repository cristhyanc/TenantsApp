using System;
using System.Collections.Generic;
using System.Linq.Expressions;
using System.Text;

namespace TenantsApp.Entities.Interfaces
{
    //private interface IRepository<T> where T : class, new()
    //{

    //    T Get(Guid id);
    //    T Get(Expression<Func<T, bool>> predicate);

    //    List<T> GetAll();
    //    List<T> GetAll(Expression<Func<T, bool>> predicate = null);
      
    //    int Create(T entity);
    //    int Create(IEnumerable<T> entities);
    //    int Modify(T entity);
    //    int Modify(IEnumerable<T> entities);
    //    int Delete(T entity);
    //    int Delete(IEnumerable<T> entities);
    //}
}
